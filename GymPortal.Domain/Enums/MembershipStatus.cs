namespace GymPortal.Domain.Enums;
// Denna enum representerar olika status för ett medlemskap i gymmet, såsom aktivt, avbrutet eller utgånget.
public enum MembershipStatus
{
    Active = 1,
    Cancelled = 2,
    Expired = 3
}